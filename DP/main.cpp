#include "include.h"

int main () {
    vector<int> nums = {1,0};
    int amount = 5;
    vector<int> coins = {1, 2, 5};
    solution518_1(amount,coins);
    solution518_2(amount,coins);
    return 0;
}